export const zonasPorFase = () => {
  return {
    1: [
      {
        x: window.innerWidth * 0.001,
        y: window.innerHeight * 0.36,
        width: window.innerWidth * 0.39,
        height: window.innerHeight * 0.12
      },
      {
        x: window.innerWidth * 0.001,
        y: window.innerHeight * 0.735,
        width: window.innerWidth * 0.39,
        height: window.innerHeight * 0.12
      },
      {
        x: window.innerWidth * 0.605,
        y: window.innerHeight * 0.735,
        width: window.innerWidth * 0.39,
        height: window.innerHeight * 0.12
      },
      {
        x: window.innerWidth * 0.605,
        y: window.innerHeight * 0.36,
        width: window.innerWidth * 0.39,
        height: window.innerHeight * 0.12
      },
      {
        x: window.innerWidth * 0.077,
        y: window.innerHeight * 0.001,
        width: window.innerWidth * 0.2,
        height: window.innerHeight * 0.1
      },
      {
        x: window.innerWidth * 0.35,
        y: window.innerHeight * 0.001,
        width: window.innerWidth * 0.25,
        height: window.innerHeight * 0.1
      },
      {
        x: window.innerWidth * 0.8,
        y: window.innerHeight * 0.15,
        width: window.innerWidth * 0.135,
        height: window.innerHeight * 0.127
      }
    ],
    2: [
      {
        x: window.innerWidth * 0.96,
        y: window.innerHeight * 0.12,
        width: window.innerWidth * 0.05,
        height: window.innerHeight * 0.665
      }
    ],
    3: [],
    4: [],
    5: []
  }
}
